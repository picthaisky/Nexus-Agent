pydantic>=2.0,<3.0
pydantic-settings>=2.2,<3.0
python-dateutil>=2.9.0.post0
pytest>=7.0
pytest-cov>=7.1.0
pytest-asyncio>=0.23.0
openai>=1.0.0
langchain-openai>=0.1.0
fastapi>=0.110.0
uvicorn[standard]>=0.48.0
httpx>=0.27.0
websockets>=12.0
ddgs>=9.14.0

# ── Security & rate limiting ────────────────────────────────────────────────
slowapi>=0.1.9

# ── Reliability ─────────────────────────────────────────────────────────────
tenacity>=8.2.3
pybreaker>=1.0.2

# ── Data & cache ────────────────────────────────────────────────────────────
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
redis>=5.0

# ── Observability ───────────────────────────────────────────────────────────
prometheus-client>=0.20.0
sentry-sdk[fastapi]>=2.7.0

# ── Optional multi-provider LLM clients (install only if you use the provider) ──
anthropic>=0.30.0
google-generativeai>=0.7.0
# vllm>=0.4.0  # Linux + CUDA only — install separately on GPU server

# ── Scheduling & async file I/O ─────────────────────────────────────────────
APScheduler>=3.10.0
aiofiles>=23.0

# ── Email & notifications ───────────────────────────────────────────────────
# smtplib is built-in; no extra package needed for email
# LINE Notify uses httpx (already listed above)

# ── File parsing ─────────────────────────────────────────────────────────────
python-multipart>=0.0.9    # required for FastAPI file uploads
Jinja2>=3.1.6              # email templates

# ── Data export ─────────────────────────────────────────────────────────────
openpyxl>=3.1.0            # Excel export
