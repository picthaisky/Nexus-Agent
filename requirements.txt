pydantic>=2.0,<3.0
python-dateutil>=2.8
pytest>=7.0
pytest-cov>=4.0
openai>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
# vllm>=0.4.0  # Linux + CUDA only — install separately on GPU server
